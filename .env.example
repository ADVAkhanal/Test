# Copy this to .env for local development
# Railway sets DATABASE_URL automatically when you add a Postgres plugin

PORT=3000

# PostgreSQL connection string
# Local example:
DATABASE_URL=postgresql://postgres:password@localhost:5432/amf_kpi

# Railway provides this automatically — you don't need to set it manually in Railway
